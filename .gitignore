HELP.md
target/
.mvn/wrapper/maven-wrapper.jar
!**/src/main/**/target/
!**/src/test/**/target/

### STS ###
.apt_generated
.classpath
.factorypath
.project
.settings
.springBeans
.sts4-cache

### IntelliJ IDEA ###
.idea
*.iws
*.iml
*.ipr

### NetBeans ###
/nbproject/private/
/nbbuild/
/dist/
/nbdist/
/.nb-gradle/
build/
!**/src/main/**/build/
!**/src/test/**/build/

### VS Code ###
.vscode/


docs/assets
docs/LINKEDIN-POST.md
# runtime state — data/ 루트의 런타임 파일만 (data/vehicle 샘플 CSV는 아래 블록에서 커밋 허용)
/data/*.json
/data/*.parquet
/data/*.crc
/data/*.mv.db
/data/*.db
# Phase 1·2 적용 후 신규 런타임 저장 디렉터리(파티션 Parquet / JSONL hot) — 커밋 금지
/data/articles/
/data/articles_hot/
.DS_Store
*.crc

# 시크릿 — 절대 커밋 금지 (.env.example 템플릿은 커밋)
.env
!.env.example

# 개인/포트폴리오 메타 — 로컬 보관, 공개 repo 제외 (인터뷰 정리·링크드인 자료)
docs/CASE-STUDY.md
docs/LINKEDIN-POST.md
docs/card1_architecture.png
docs/card4_results.png
docs/assets/linkedin/

.fuse_hidden*

run.log
run-hotspot.log
.fuse_hidden*
*.log
run*.log

# IBM Semeru / OpenJ9 JVM 진단 덤프 (pkill -9 등으로 생성 — 레포에 넣지 않음)
javacore.*
Snap.*
jitdump.*
core.*
*.trc
*.dmp
*.phd
heapdump.*

# Vehicle 데이터: 매뉴얼 원문(저작권·대용량)·원본 JSON은 제외, 공개·샘플 CSV만 커밋(데모 재현용)
data/vehicle/**
!data/vehicle/
!data/vehicle/recalls/
!data/vehicle/recalls/*.csv
!data/vehicle/complaints/
!data/vehicle/complaints/*.csv
!data/vehicle/parts/
!data/vehicle/parts/*.csv
!data/vehicle/inspection/
!data/vehicle/inspection/*.csv

# KB 리셋 백업 (커밋 금지)
data_backup_*/

# 매뉴얼 중복 백업 (정규화 시 생성)
_manuals_dups/
data/vehicle/_manuals_dups/

# 파일 백업/타임스탬프 (.bak, .bak.20260624_... 등)
*.bak
*.bak.*

# 대용량 발표/참고 자료 — 레포 비대 방지(로컬 보관). 작은 샘플 CSV/문서는 커밋 허용
sample/*.pptx
sample/*.pdf

# 참고용 외부 보고서(저작권·대용량) — 커밋 금지
현대자동차 지속가능성 보고서/
*지속가능성 보고서*/

# ML 산출물 (무거움 — 커밋 금지, 어댑터는 작아 선택)
ml/.venv/
ml/finetune/merged/
ml/finetune/adapters/*.safetensors
ml/optimize/gguf/
ml/data/out/
ml/**/__pycache__/

# Frontend 빌드물 (frontend/.gitignore도 있지만 안전장치)
frontend/node_modules/
frontend/.next/
frontend/out/

# 에이전트/하니스 로컬 상태 — 절대 커밋 금지
.claude/
.omc/
CLAUDE.md.fablize-bak.*

# Python 캐시 (ml/ 외부 디렉터리도 모두)
**/__pycache__/

# omc
.omc
CLAUDE.mdㅂ