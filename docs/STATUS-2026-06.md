# 프로젝트 상태 인덱스 (2026-06) — "내가 놓친 것" 캐치업

터미널 Claude + Cowork 양쪽에서 작업이 누적돼 길을 잃기 쉬워, **무엇이 이미 들어갔고 /
무엇이 남았고 / 어디를 보면 되는지**를 한 곳에 모은다. (문서 63개의 내비게이션 겸용)

---

## 1. 최근 출하(커밋) — 테마별

| 테마 | 핵심 | 커밋 |
|---|---|---|
| **인제스트 스케일** | hot/cold **파티션화**(문서별 parquet) + 측정 + 병렬 임베딩 + dedup 권위화(카탈로그) | c3ced6d |
| **매뉴얼 메타/RAG 정확도** | manual-meta **백필**(차종·연식·연료·언어) + 하이브리드/RAGAS 로드맵 + NHTSA 새로고침 | ab6edcb |
| **케이스 중심 재구성** | 트리아지 **영속화**(해결/페이지네이션) + **접수번호별 리포트 적재**(캐시·정비사 메모) | 2b3b168 |
| **보안** | scripts의 **TLS 검증 비활성화 폴백 제거**(MITM 위변조 차단) + 시큐리티 리뷰 | 191ddeb, 652217e |
| **테스트** | Phase 1·2 회귀(JSONL hot·Parquet 파티션·Tiered) + ManualMeta 단위테스트 | 5a3727d, 13c915b |
| **설정/평가** | YAML 중복 키 통합 + RAGAS T0 베이스라인·측정 가이드 | e71edb0 |
| **그 이전(요약)** | 분산학습 셋업·견적(부가세)·qwen3 기본·분석 대시보드·정비 캘린더·멀티턴 챗·차종 이미지·문서전용 어시스턴트·DPO·온디바이스 TTS | 69e7c3b 외 |

> 즉, Cowork에서 논의한 **파티션 parquet·케이스 영속·NHTSA 재수집·보안 폴백 제거**는 **이미 git에 반영**됨
> (터미널이 더 완전한 형태로 커밋: ArticleParquetStore에 carCode/carModel/powertrain/year/lang/region 메타까지).

---

## 2. 문서 지도 (63개 → 7묶음)

- **시작/인수인계**: `HANDOVER.md`(30분 온보딩), `README.md`, `ARCHITECTURE.md`, `VEHICLE_ARCHITECTURE.md`, `SDS.md`, `DESIGN.md`
- **RAG·인제스트**: ⭐`INGEST-BOTTLENECKS.md`(병목 캐논), `RAG-MANUAL-METADATA.md`, `RAG-HYBRID-RETRIEVAL.md`, `RAG-ACCURACY-ROADMAP.md`, `CHUNKING.md`, `EMBEDDINGS.md`, `RERANKING.md`, `PGVECTOR.md`, `INGESTION-FORMATS.md`
- **데이터·거버넌스**: `DATA-MODEL.md`, `ERD.md`, `GOVERNANCE.md`, `DEEP-DIVE-DATA-GOVERNANCE.md`, `TABULAR-SQL.md`, `MODEL-REGISTRY.md`
- **평가**: `EVALUATION.md`, `RAG-EVAL-RAGAS.md`, `RESULTS.md`, `VERIFICATION.md`
- **학습/모델**: `WHY_LORA.md`, `PEFT_METHODS.md`, `DPO_ALIGNMENT.md`, `RAG_COMPONENT_FINETUNING.md`, `INFERENCE_OPTIMIZATION.md`, `SERVING.md`, `LLM-ABSTRACTION.md`
- **운영/배포**: `RUNBOOK.md`, `OPERATIONS.md`, `AS-OPERATIONS.md`, `DEPLOYMENT.md`, `CLOUD-DEPLOYMENT.md`, `DEPLOY-ORACLE.md`, `CICD.md`, `MONITORING*.md`, `DEBUGGING.md`, `HOTSPOT-RUNTIME.md`, `H2-CONSOLE.md`, `WATSONX-SETUP.md`
- **인사이트/기획**: `DECISIONS.md`, `CASE-STUDY.md`, `HYUNDAI_NEEDS_ROADMAP.md`, `RAG-LANDSCAPE.md`, `MULTIMODAL.md`, `VOICE_ONDEVICE_VISION.md`, `GRAPHRAG_VEHICLE.md`, `SECURITY*.md`, `LINKEDIN-POST.md`

---

## 3. 중복 정리 대상 (Cowork가 만든 것 ⊂ 터미널 캐논)
- `RAG-INGEST-SCALING.md`, `SESSION-INSIGHTS-2026-06.md`(Cowork 작성)는 ⭐`INGEST-BOTTLENECKS.md`(터미널, 436줄)와 **내용 중복**.
  → **INGEST-BOTTLENECKS.md를 캐논으로** 두고, 앞 둘은 포인터/요약으로 강등(또는 삭제). 이 문서 상단에 안내 추가함.

---

## 4. 미커밋 / 정리 필요 (지금 상태)
- 변경됨(미커밋): `data/vehicle/complaints|recalls/*.csv`(NHTSA 재수집 결과), `ml/optimize/bench.csv`, `sample/nasa.csv`
- 스트레이: 루트의 한글명 폴더 `현대자동차 지속가능성 보고서/`(untracked)
- ⚠️ **대용량이 git에 들어감** — 방금 force-push가 75MB. `data/vehicle/manuals/`(458권, 수백MB~GB)·`data/articles/`(parquet)는 **`.gitignore` 처리 권장**(GitHub 100MB 제한·클론 비대). 이미 들어간 건 history에서 빼는 정리 필요할 수 있음.

---

## 5. 다음 (RAG 운영화 — 우선순위)
1. **pgvector 전환** — `docker compose up -d` + `VECTOR_STORE=pgvector`. 인메모리 → 실제 벡터DB(영속·ANN). 코드 준비됨.
2. **exit 255(OOM) 근본 해결** — 임베딩 `float[]` 원시화(박싱 제거 ~1/4) + loadAll 전건 적재 제거 + 부팅 rebuild 가드.
3. **임베딩 배치/비동기 워커** — 적재를 요청·부팅 경로 밖으로.
4. **(선택) Dagster/Prefect 파이프라인** — `fetch→download→extract→chunk→embed→upsert` asset, 재시도·백필·관측. JD용 산출물.
5. **하이브리드(BM25+벡터) + 리랭커** — 메타 1차 필터 위에 2·3단(RAG-MANUAL-METADATA 로드맵).

---

## 6. 즉시 할 일 (한 줄)
- [ ] `.gitignore`에 `data/vehicle/manuals/`, `data/articles/`, `*.bak.*` 추가 + 대용량 history 정리
- [ ] 미커밋 데이터 CSV 커밋 여부 결정(재수집본 반영할지)
- [ ] pgvector 전환 검증(`article_vectors` count)
- [ ] 선별 ingest(케이스 차종 + 신형/EV)로 KB 채우고 RAG 근거 확인
