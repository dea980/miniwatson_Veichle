# DevLog — 2026-07-03

한 세션의 작업 이력을 시간순으로 정리한다. **실제로 한 일**, **왜**, **결과(증거)**, 그리고 중간에 낀 오입력(noise)까지 그대로 남겨 재현·인수인계가 되게 한다.

산출물 요약:
- 신규 테스트 `src/test/java/com/miniwatson/security/ApiKeyAuthFilterTest.java` (7 케이스)
- 신규 문서 `docs/SESSION-SECURITY-TEST-2026-07-03.md` (보안 검증 상세)
- 신규 문서 `docs/DEVLOG-2026-07-03.md` (이 파일)

---

## 타임라인

### T0 — 세션 시작, 방향 탐색 (noise)
- `/brainstorming`(인자 없음) 진입 → 프로젝트 컨텍스트(README·git log·docs) 스캔.
- `/model` → **Fable 5**로 기본 모델 변경. (환경 설정, 코드 무관)
- `./docker`, `./ doctor` → **오입력**. 슬래시 명령 오타로 판단, 의도 확인함.

> 이 구간은 산출물 없음. "무엇을 할지" 정하는 탐색 단계.

### T1 — 주제 확정: 도커 배포 브레인스토밍 (중단됨)
- 요청: "도커 배포 구성 브레인스토밍 하자".
- 현행 파악: `Dockerfile`, `docker-compose{,.prod,.watsonx,.monitoring}.yml`, `docs/DEPLOYMENT.md`·`CLOUD-DEPLOYMENT.md`·`DEPLOY-ORACLE.md` 읽음.
- 목표 질문(멀티셀렉트) → 답변: 풀스택 완결 + 실제 클라우드 배포 + compose 통합 + 신규 컴포넌트 반영 + **스크립트로 Oracle free tier 배포**.
- **직후 사용자가 방향 전환**: "Nope … lets test the security part".

> 브레인스토밍은 여기서 **중단**. 도커 배포 설계는 미착수(다음 세션 후보). 관련 태스크는 정리(삭제)함.

### T2 — 보안 검증: 사전 조사
- **왜**: `security.enabled=true`에서 인증·격리가 실제 동작하는지 관찰로 확인.
- 조사: `application.yaml`의 `security.*`, `security/` 클래스 9개, 기존 테스트.
- 파악한 모델:
  - 토글 `SECURITY_ENABLED`/`SECURITY_MODE`(기본 `apikey-filter`), 3모드(apikey-filter·spring-apikey·jwt).
  - 보호 범위 `/api/**`만, actuator·비-API는 우회(`shouldNotFilter`).
  - 실패 = **fail-closed 401**, 키 비교 **상수시간**(`MessageDigest.isEqual`).
  - 키: `dev-all:"*"`(전권), `acme:"default,kr-bcg"`(테넌트).

### T3 — 라이브 서버 테스트 (1차, 실패 — 환경)
- `SECURITY_ENABLED=true ./mvnw spring-boot:run` → health 200까지 확인(52s 기동).
- 6개 curl 시나리오(키없음/오답/유효/빈헤더/actuator/비-API) → **전부 `000`**.
- 원인 추적: 로그 `exit 134 = SIGABRT`. 첫 요청 직후 JVM 네이티브 크래시.
- `hs_err_pid9600.log`: `SIGSEGV @ resource_allocate_bytes`, `bsd-amd64 emulated-client`.
- **판정**: 보안 버그 아님 → **Apple Silicon에서 x86_64 JVM을 Rosetta 에뮬레이션**하는 로컬 환경 문제(SDKMAN current가 x86_64 JDK). `docs/HOTSPOT-RUNTIME.md`의 크래시 계열과 동성격.

### T4 — 격리 규칙 단위테스트 (2차, 통과)
- **왜**: flaky 라이브 대신 결정적 경로.
- `./mvnw -Dtest='*Tenant*,*ApiKey*,*Security*,*Jwt*' test` → `TenantGuardTest` **5/5 통과**.
- 검증된 불변식: 허용집합 통과/거부, wildcard, null·빈집합 거부(fail-closed), 빈 namespace→default 폴백.

### T5 — 세션 문서화 요청 대응
- 요청: "왜·어떻게 했는지 단계별 문서화".
- `docs/SESSION-SECURITY-TEST-2026-07-03.md` 작성(사전조사→라이브시도→크래시규명→단위테스트→결론·재현명령).

### T6 — 인증 필터 인프로세스 검증 (3차, 통과) ← 갭 닫음
- **왜**: 라이브가 환경 탓에 flaky → 필터 401/통과를 컨텍스트 없이 결정적으로.
- **어떻게**: 전체 `@SpringBootTest`(무겁고 크래시 위험) 대신 **standalone MockMvc**로 `ApiKeyAuthFilter`만 격리. `SecurityProperties` 손 구성 + 더미 컨트롤러(`/api/probe`,`/public`).
- `src/test/java/com/miniwatson/security/ApiKeyAuthFilterTest.java` (7 케이스).
- `./mvnw -Dtest='ApiKeyAuthFilterTest' test` → **7/7 통과, 1.8s, 크래시 없음**.
- 문서에 §6로 반영, 검증 상태표 갱신.

### T7 — noise
- "open discord" → 채팅 채널 관련 입력. 코드 작업과 무관, 무시.

---

## 세션 결과 (증거 기준)

| 항목 | 상태 | 증거 |
|---|---|---|
| 격리 규칙(`TenantGuard`) | ✅ | `TenantGuardTest` 5/5 |
| 인증 필터 401/통과/우회/토글 | ✅ | `ApiKeyAuthFilterTest` 7/7 (1.8s) |
| 라이브 HTTP 소켓 e2e | ⚠️ 미검증 | 로컬 x86_64 JVM 크래시(환경), 보안코드 무관 |
| 도커 배포 브레인스토밍 | ⏸ 미착수 | 방향 전환으로 중단 |

## 다음 세션 후보
1. 도커 배포 브레인스토밍 재개 — 목표는 이미 수집됨(풀스택+Oracle free tier 스크립트 배포). → `/brainstorming`.
2. 라이브 HTTP e2e — arm64 JDK(`/usr/libexec/java_home -a arm64`) 또는 컨테이너(`Dockerfile`은 arm64 HotSpot)에서 §T3 curl 재실행.
3. 로컬 JVM 정리 — SDKMAN current를 arm64 21로 전환하면 라이브 실행 크래시 근절.

## 커밋 제안
아직 커밋 안 함. 원하면:
```
test: ApiKey 인증 필터 인프로세스 검증(7케이스) + 보안 검증/데브로그 문서
```
대상: `ApiKeyAuthFilterTest.java`, `SESSION-SECURITY-TEST-2026-07-03.md`, `DEVLOG-2026-07-03.md`
